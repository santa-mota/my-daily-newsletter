"""
Tool definitions for the LLM agent to fetch news sources.

Each tool is a callable function that the agent can invoke to gather fresh content.
Tools are registered with the agent and exposed via function calling (OpenAI format).
"""
